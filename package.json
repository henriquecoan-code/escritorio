{
  "name": "ob-dashboard-rede",
  "private": true,
  "version": "1.0.0",
  "description": "Testes automatizados do dashboard OB",
  "scripts": {
    "serve": "python3 -m http.server 8000",
    "test:e2e": "playwright test",
    "test:rules": "firebase emulators:exec --only firestore \"vitest run tests/firestore.rules.test.mjs\"",
    "test:security": "echo Rode o comando do OWASP ZAP descrito no README.md"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "@playwright/test": "^1.53.0",
    "firebase": "^11.0.0",
    "firebase-tools": "^14.5.0",
    "vitest": "^2.1.8"
  }
}
